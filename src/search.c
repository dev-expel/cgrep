#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "../include/cgrep.h"

char* get_valid_text(char* file_text, char* search_term, const int MAX_FILE_SIZE) {
    char* valid_text = (char*) malloc(MAX_FILE_SIZE * sizeof(char));

    return valid_text;
}